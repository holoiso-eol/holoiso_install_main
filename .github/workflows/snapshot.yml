name: Build snapshot
on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: archlinux:latest
      options: --privileged
    steps:
      - uses: actions/checkout@v2
      
      - name: Prepare pacman keyring
        run: |
          pacman-key --init
          pacman --noconfirm -Sy archlinux-keyring
          
      - name: Build ISO File
        run: |
          su -s /bin/bash nobody -c "makepkg -s"

      - uses: actions/upload-artifact@v2
        with:
          name: holoiso
          path: holoiso*.tar.zst
